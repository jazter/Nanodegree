<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jazter.myappportfolio" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".spotify.activities.SearchActivity"
            android:label="@string/title_activity_spotify_search" >
        </activity>
        <activity
            android:name=".spotify.activities.TracksActivity"
            android:label="@string/title_activity_spotify_tracks" >
        </activity>
        <activity
            android:name=".spotify.activities.PlayActivity"
            android:label="@string/title_activity_play" >
        </activity>
    </application>

</manifest>
